# Boxing Class Coach Tools

**Ring Call** is in [`boxing-games/`](./boxing-games/) — projector-first tools for running a live boxing class (guided sessions, pad caller, drill board, team battle).

```bash
cd boxing-games && npm install && npm run dev
```

Cast to the gym TV. Space / Enter advances from the coach device.
