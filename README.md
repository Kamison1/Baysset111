# Boxing Class Games

**Ring Call** lives in [`boxing-games/`](./boxing-games/) — five interactive games for boxing coaches to run warmer, sharper, more competitive classes.

```bash
cd boxing-games && npm install && npm run dev
```
